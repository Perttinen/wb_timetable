name: pipeline

on:
  push:
    branches:
      - dev

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check style
        run: npm run lint:fix
